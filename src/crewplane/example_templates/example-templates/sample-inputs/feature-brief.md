Build a small, well-tested change that improves the first-run workflow
experience. The implementation should keep generated templates deterministic,
avoid provider-specific assumptions in core workflow logic, and include a clear
validation path that can run with the mock invoker.
