# GitHub Copilot Instructions

Use `AGENTS.md` as the canonical repository instructions.

Copilot-specific note:
- Keep this file thin. Put repo-wide instructions in `AGENTS.md`.
- Use `DEVELOPMENT.md` for setup, validation, release, and automation guidance.
- Do not duplicate project rules here; update `AGENTS.md` instead.
